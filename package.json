{
  "name": "spaceai",
  "version": "0.1.0",
  "description": "A from-scratch decoder-only language model written in TypeScript. Browser-first.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "demo": "tsx scripts/bpe-demo.ts",
    "build": "node scripts/build-browser.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.28.2",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
